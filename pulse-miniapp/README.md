# Pulse Mini App Frontend

Це фронтенд частина Mini App для Telegram бота Pulse.

## 🛠 Технології
- **Vite** + **React** + **TypeScript**
- **Tailwind CSS v4** (Styling)
- **Zustand** (State Management)
- **Framer Motion** (Animations)
- **Lucide React** await(Icons)

## 🚀 Запуск локально

1. Встановіть залежності (якщо ще ні):
   ```bash
   npm install
   ```

2. Запустіть сервер розробки:
   ```bash
   npm run dev
   ```
   Додаток буде доступний за адресою: `http://localhost:5173/`

## 📦 Збірка (Build)

Для підготовки до деплою:
```bash
npm run build
```
Результат буде у папці `dist/`.

## 🔗 Інтеграція з API

За замовчуванням клієнт шукає API за адресою `http://localhost:8000/api/v1`.
Ви можете змінити це у файлі `.env` (створіть його):
```
VITE_API_BASE_URL=https://your-api-domain.com/api/v1
```

## 📱 Telegram WebApp

Для тестування в Telegram:
1. Використовуйте **ngrok** або аналог для створення HTTPS тунелю до локального хоста.
2. Додайте URL у налаштування бота через BotFather (Menu Button -> Web App).
