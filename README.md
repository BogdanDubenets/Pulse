# Pulse News Bot 💓

Проект для автоматичного збору та резюмування новин з Telegram.

## Швидкий запуск

### 1. База даних (Supabase)
Переконайтеся, що Docker запущений.
```bash
npx supabase start --ignore-health-check
```
*Ми використовуємо `--ignore-health-check`, оскільки сервіс `mailpit` може мати проблеми з архітектурою на Windows, але це не впливає на роботу БД.*

### 2. Налаштування середовища
Відредагуйте файл `.env`. Вам потрібно додати:
- `BOT_TOKEN` (від BotFather)
- `API_ID` та `API_HASH` (з my.telegram.org)
- `GEMINI_API_KEY`

### 3. Запуск бота
```bash
.venv\Scripts\python -m bot.main
```

## Структура проекту
- `bot/` — логіка Telegram бота (aiogram 3.x)
- `config/` — налаштування та завантаження .env
- `database/` — моделі SQLAlchemy та підключення до БД
- `services/` — бізнес-логіка (моніторинг, AI, кластеризація)
- `supabase/` — міграції та конфігурація Supabase
