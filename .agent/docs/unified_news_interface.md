# Unified News Interface (Pulse Architecture)

## Огляд
Стандарт Unified News Interface забезпечує єдиний візуальний та технічний досвід перегляду новин у Mini App, незалежно від типу контенту (Story або Brief).

## Ключові принципи

### 1. Формат списку (List View)
Всі елементи списку повинні використовувати компонент `DigestListItem` та відповідати формату:
- **[Час] [Заголовок]**: Наприклад, `14:30 Повітряна тривога в Києві...`
- **Маркування**: Сюжети (Stories) позначаються іконкою 🔥 та мають бейдж "Сюжет".
- **Джерела**: Для сюжетів відображається кількість задіяних каналів (наприклад, "5 джерел").

### 2. Детальний перегляд (Detail View)
Вікно деталей повинно містити:
- **AI-Summary**: Стислий та структурований виклад події.
- **Джерела (Multi-source)**: Список усіх оригінальних каналів з іконками та посиланнями.
- **Чистий текст**: Видалення Markdown-посилань та технічного сміття з тексту.
- **Навігація**: Обов'язкова кнопка "Назад до дайджесту" для повернення до списку.

## Технічна реалізація

### Backend (Python/FastAPI)
- Кожен об'єкт новини повинен мати унікальний `uid` (формат: `story_{id}` або `brief_{id}`).
- Обов'язкові поля: `uid`, `type`, `title`, `summary`, `sources` (список об'єктів `name`/`url`).
- Синхронізація через Pydantic-моделі `StoryCompact` та `BriefNews`.

### Frontend (React/TypeScript)
- Використання інтерфейсу `DigestItem` для обгортки даних.
- Спільний обробник `handleMoreClick` для навігації між категоріями та часовою шкалою.

## UX Рекомендації
- Використовувати скляний морфізм (`backdrop-blur`) для модальних вікон.
- Плавні анімації появи та переходів.
- Адаптивність під різні екрани мобільних пристроїв.
